<p id="notice"><%= notice %></p>
<% if current_admin %>
    <table class="ui fixed single line celled table">
        <thead>
            <tr><h1 class="instructors">Instructors</h1></tr><%=link_to "Add", new_instructor_path(@instructor), :class=> "ui tiny olive button"%>
            <table>
              <tr>
                <th>First name</th>
                <th>Last name</th>
                <th>Salary</th>
                <th>Education</th>
                <th>Age</th>
              </tr>
            </thead>
            <h1>Instructors</h1>
    <tbody>
        <% @instructors.each do |instructor| %>
                
        <tr id="instructor-<%= instructor.id %>">
            <td>
                <a class="item">
                <img class="ui mini circular image" src="https://www.shareicon.net/data/128x128/2016/07/05/791221_man_512x512.png">
                </a>
                <a href="/instructors/<%= instructor.id %>">
                <td><%= instructor.first_name %></td>
                <td><%= instructor.last_name %></td>
                <td><%= instructor.salary %></td>
                <td><%= instructor.education %></td>
                <td><%= instructor.age %></td></a>
            </td>  
            <% if instructor.cohorts.nil? %>
            <p> not availiable <p>
            <% else %>
             <% @cohort= instructor.cohorts %>
            <td><a href="/cohorts/<%= @cohort_id %>"> <%= @cohort.name %> </a></td>
             <% end %>4
                <td> 
                    <%= link_to 'Show', instructor, :class=> "ui tiny olive button"%>
                    <%= link_to 'Edit', edit_instructor_path(instructor), :class=> "ui tiny olive button"%>
                    <%= link_to 'Delete', instructor_path(instructor), method: :delete, remote:true, data: { confirm: 'Are you sure?' }, :class=> "ui tiny olive button" %>
                </td>
                        
            <% end %>
        </tr>
    </tbody>
    </table>
<% end %>




<br>

<%= link_to 'New Instructor', new_instructor_path %>
